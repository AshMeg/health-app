# Components

- `ui/` — shadcn primitives. Do not edit generated files by hand.
- `layout/` — app shell, sidebar, top bar, and other chrome.
- `shared/` — small reusable presentational components used across features.

Feature-specific components live in `src/features/<module>/components/`.
